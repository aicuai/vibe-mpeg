{
  "name": "concat",
  "description": "Concatenate multiple video files into one",
  "ffmpeg": true,
  "parameters": {
    "type": "object",
    "required": ["files"],
    "properties": {
      "files": {
        "type": "array",
        "description": "List of video file paths to concatenate in order",
        "items": { "type": "string" }
      },
      "output": { "type": "string", "description": "Output file path" },
      "reencode": { "type": "boolean", "default": false, "description": "Re-encode instead of stream copy (slower but handles mixed formats)" }
    }
  }
}
