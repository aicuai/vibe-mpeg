{
  "name": "demo",
  "description": "vibe-mpeg overview video (6 scenes, 18s) — used in README",
  "template": "demo",
  "defaultDuration": 18,
  "parameters": {
    "type": "object",
    "properties": {
      "output": { "type": "string", "default": "out/demo.mp4" }
    }
  }
}
